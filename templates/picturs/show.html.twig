{% extends 'base.html.twig' %}

{% block title %}Picturs{% endblock %}

{% block body %}
  <h1>Picturs</h1>

  <table class="table">
    <tbody>
      <tr>
        <th>Id</th>
        <td>{{ pictur.id }}</td>
      </tr>
      <tr>
        <th>Name</th>
        <td>{{ pictur.name }}</td>
      </tr>
    </tbody>
  </table>

  <a href="{{ path('app_picturs_index') }}">back to list</a>

  <a href="{{ path('app_picturs_edit', {'id': pictur.id}) }}">edit</a>

  {{ include('picturs/_delete_form.html.twig') }}

  <hr>

{% endblock %}
